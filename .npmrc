# The @app-design-system scope lives in GitHub Packages, not npmjs.org.
# Consuming repos need this same line (see the root README).
@app-design-system:registry=https://npm.pkg.github.com

# Publishing from dist/ via publishConfig.directory relies on pnpm resolving
# workspace: protocol versions at pack time.
link-workspace-packages=true
