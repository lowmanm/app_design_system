// No runtime TypeScript API - this package's product is Sass. Import the
// entry point for your business unit's brand into your app's global
// styles, e.g. `@use '@app-design-system/theme-angular-material/themes/azure-blue'`.
// Note that it must be loaded alongside @app-design-system/tokens' CSS,
// which supplies the --color-* variables this package's themes reference.
// See README.md.
export {};
