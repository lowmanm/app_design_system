/** Shared size scale, consistent across every component in the library. */
export type ComponentSize = 'sm' | 'md' | 'lg';

/** Shared visual-emphasis scale, mirroring Material's filled/outlined/text button styles. */
export type ComponentVariant = 'filled' | 'outlined' | 'text';
