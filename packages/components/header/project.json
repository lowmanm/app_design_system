{
  "name": "header",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/components/header/src",
  "prefix": "brk",
  "projectType": "library",
  "tags": ["scope:angular"],
  "implicitDependencies": ["core"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["core:build"],
      "outputs": ["{workspaceRoot}/dist/packages/components/header"],
      "options": {
        "command": "ng-packagr -p ng-package.json -c tsconfig.package.json",
        "cwd": "packages/components/header"
      },
      "cache": true,
      "inputs": ["{projectRoot}/src/**/*", "^production"]
    }
  }
}
