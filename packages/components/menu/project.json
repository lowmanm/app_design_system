{
  "name": "menu",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/components/menu/src",
  "prefix": "brk",
  "projectType": "library",
  "tags": ["scope:angular"],
  "implicitDependencies": ["core", "button", "icon"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["core:build", "button:build"],
      "outputs": ["{workspaceRoot}/dist/packages/components/menu"],
      "options": {
        "command": "ng-packagr -p ng-package.json -c tsconfig.package.json",
        "cwd": "packages/components/menu"
      }
    }
  }
}
